# frontend

Static site served by Nginx as a placeholder. Replace with your SPA (React/Vite/etc.).

## Run (Docker)

```bash
docker build -t ecommerce-frontend:dev .
docker run --rm -p 8080:80 ecommerce-frontend:dev
```

Open http://localhost:8080
